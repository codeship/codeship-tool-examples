compiledemo:
  build: ./
  dockerfile: Dockerfile.build
  volumes:
    - ./tmp:/data
deploy:
  build: ./
  dockerfile: Dockerfile
  volumes_from:
    - compiledemo
app:
  build: ./
  dockerfile: Dockerfile.app
